import LoginBackgroundView from 'src/sections/auth/login-background-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Iniciar sesión RCA',
};

export default function LoginBackgroundPage() {
  return <LoginBackgroundView />;
}
